import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles((theme) => ({
  card: {
    width: `calc(100% - 60px)`,
    height: `250px`,
  },
  box: {
    height: `100%`,
    display: `flex`,
    backgroundColor:
  },
}));
