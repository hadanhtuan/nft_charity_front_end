import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  toolbar: {
    height: "64px",
  },

  logo: {
    flexGrow: "1",
    cursor: "pointer",
    textDecoration: "none",
    fontSize: "20px",
  },
}));
