import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  appbar: {
    position: "static",
    color: `#00261C`,
    backgroundColor: `red`,
    boxShadow: `none`,
    anchor: `top`,
    height: `80px`,
  },
}));
