import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  widget: {
    display: "inline-flex",
    width: `100px`,
    height: `100px`,
    borderRadius: `16px`,
    padding: `24px`,
    gap: `16px`,
    justifyContent: `space-between`,
  },
}));
